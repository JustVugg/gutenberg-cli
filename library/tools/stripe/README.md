# stripe

Generated by Gutenberg as a Go-first agent-native tool.

## Quick Start

```bash
go test ./...
go run ./cmd/stripe operations
go run ./cmd/stripe call GetAccount
go run ./cmd/stripe mcp
```

## Product Thesis

stripe should become a local, searchable operational workspace, not only an endpoint wrapper.

Write and destructive operations dry-run until `--yes` is passed.
