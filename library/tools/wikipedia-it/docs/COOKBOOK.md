# wikipedia-it Cookbook

```bash
go run ./cmd/wikipedia-it operations
go run ./cmd/wikipedia-it call getPageSummaryBologna
go run ./cmd/wikipedia-it sync getPageSummaryBologna
go run ./cmd/wikipedia-it search status
go run ./cmd/wikipedia-it call getPageSummaryBologna --data '{"example":true}'
go run ./cmd/wikipedia-it mcp
```
