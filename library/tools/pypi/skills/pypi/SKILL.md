---
name: pypi
description: pypi should become a local, searchable operational workspace, not only an endpoint wrapper. Use for 2 operation/source entries against https://pypi.org.
---

# pypi

Generated by Gutenberg. Use this skill to call the pypi API through the local `pypi` CLI.

## When to use

- The user asks for data exposed by pypi (pypi).
- An agent task needs structured JSON from https://pypi.org.
- You need to perform a write/destructive operation on pypi — always preview first, run with `--yes` only on explicit user confirmation.

## How to invoke

The skill assumes the generated CLI is on PATH (or invoke via `scripts/use-go.sh run ./cmd/pypi` from the project directory).

```bash
pypi operations
pypi call getPypiRequests --json
pypi sync --json
pypi search "<keyword>" --json
```


## Authentication

No authentication required.

## Operations index

- `getPypiRequests` (GET /pypi/requests/json) — read — GET /pypi/requests/json
- `getPypiNumpy` (GET /pypi/numpy/json) — read — GET /pypi/numpy/json


## Output contract

- All commands accept `--json` and emit machine-readable output.
- `call` returns `{ dryRun, operation, request, response }`. `dryRun: true` means the call was not executed (write op without `--yes`).
- `search` returns cached records. `sync` populates the local SQLite cache.

## Provenance

- Schema: gutenberg.blueprint.v1
- Generated by: gutenberg 0.3.0
- Source spec: /tmp/pypi.openapi.json
