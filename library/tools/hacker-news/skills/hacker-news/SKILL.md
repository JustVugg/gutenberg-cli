---
name: hacker-news
description: hacker-news should become a local, searchable operational workspace, not only an endpoint wrapper. Use for 5 operation/source entries against https://hacker-news.firebaseio.com.
---

# hacker-news

Generated by Gutenberg. Use this skill to call the hacker-news API through the local `hacker-news` CLI.

## When to use

- The user asks for data exposed by hacker-news (v0).
- An agent task needs structured JSON from https://hacker-news.firebaseio.com.
- You need to perform a write/destructive operation on hacker-news — always preview first, run with `--yes` only on explicit user confirmation.

## How to invoke

The skill assumes the generated CLI is on PATH (or invoke via `scripts/use-go.sh run ./cmd/hacker-news` from the project directory).

```bash
hacker-news operations
hacker-news call getTopstoriesJson --json
hacker-news sync --json
hacker-news search "<keyword>" --json
```


## Authentication

No authentication required.

## Operations index

- `getTopstoriesJson` (GET /v0/topstories.json) — read — GET /v0/topstories.json
- `getNewstoriesJson` (GET /v0/newstories.json) — read — GET /v0/newstories.json
- `getBeststoriesJson` (GET /v0/beststories.json) — read — GET /v0/beststories.json
- `getItem8863Json` (GET /v0/item/8863.json) — read — GET /v0/item/8863.json
- `getMaxitemJson` (GET /v0/maxitem.json) — read — GET /v0/maxitem.json


## Output contract

- All commands accept `--json` and emit machine-readable output.
- `call` returns `{ dryRun, operation, request, response }`. `dryRun: true` means the call was not executed (write op without `--yes`).
- `search` returns cached records. `sync` populates the local SQLite cache.

## Provenance

- Schema: gutenberg.blueprint.v1
- Generated by: gutenberg 0.3.0
- Source spec: /tmp/hacker-news.openapi.json
