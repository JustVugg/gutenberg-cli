---
name: rest-countries
description: rest-countries should become a local, searchable operational workspace, not only an endpoint wrapper. Use for 3 operation/source entries against https://restcountries.com.
---

# rest-countries

Generated by Gutenberg. Use this skill to call the rest-countries API through the local `rest-countries` CLI.

## When to use

- The user asks for data exposed by rest-countries (v3.1).
- An agent task needs structured JSON from https://restcountries.com.
- You need to perform a write/destructive operation on rest-countries — always preview first, run with `--yes` only on explicit user confirmation.

## How to invoke

The skill assumes the generated CLI is on PATH (or invoke via `scripts/use-go.sh run ./cmd/rest-countries` from the project directory).

```bash
rest-countries operations
rest-countries call getNameItaly --json
rest-countries sync --json
rest-countries search "<keyword>" --json
```


## Authentication

No authentication required.

## Operations index

- `getNameItaly` (GET /v3.1/name/italy) — read — GET /v3.1/name/italy
- `getRegionEurope` (GET /v3.1/region/europe) — read — GET /v3.1/region/europe
- `getIndependent` (GET /v3.1/independent) — read — GET /v3.1/independent


## Output contract

- All commands accept `--json` and emit machine-readable output.
- `call` returns `{ dryRun, operation, request, response }`. `dryRun: true` means the call was not executed (write op without `--yes`).
- `search` returns cached records. `sync` populates the local SQLite cache.

## Provenance

- Schema: gutenberg.blueprint.v1
- Generated by: gutenberg 0.3.0
- Source spec: /tmp/rest-countries.openapi.json
