# pokeapi

Generated by Gutenberg as a Go-first agent-native tool.

## Quick Start

```bash
go test ./...
go run ./cmd/pokeapi operations
go run ./cmd/pokeapi call getPokemon
go run ./cmd/pokeapi mcp
```

## Product Thesis

pokeapi should become a local, searchable operational workspace, not only an endpoint wrapper.

Write and destructive operations dry-run until `--yes` is passed.
