---
name: exchange-rates
description: exchange-rates should become a local, searchable operational workspace, not only an endpoint wrapper. Use for 2 operation/source entries against https://open.er-api.com.
---

# exchange-rates

Generated by Gutenberg. Use this skill to call the exchange-rates API through the local `exchange-rates` CLI.

## When to use

- The user asks for data exposed by exchange-rates (v6).
- An agent task needs structured JSON from https://open.er-api.com.
- You need to perform a write/destructive operation on exchange-rates — always preview first, run with `--yes` only on explicit user confirmation.

## How to invoke

The skill assumes the generated CLI is on PATH (or invoke via `scripts/use-go.sh run ./cmd/exchange-rates` from the project directory).

```bash
exchange-rates operations
exchange-rates call getLatestUsd --json
exchange-rates sync --json
exchange-rates search "<keyword>" --json
```


## Authentication

No authentication required.

## Operations index

- `getLatestUsd` (GET /v6/latest/USD) — read — GET /v6/latest/USD
- `getLatestEur` (GET /v6/latest/EUR) — read — GET /v6/latest/EUR


## Output contract

- All commands accept `--json` and emit machine-readable output.
- `call` returns `{ dryRun, operation, request, response }`. `dryRun: true` means the call was not executed (write op without `--yes`).
- `search` returns cached records. `sync` populates the local SQLite cache.

## Provenance

- Schema: gutenberg.blueprint.v1
- Generated by: gutenberg 0.3.0
- Source spec: /tmp/exchange-rates.openapi.json
